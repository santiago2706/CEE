import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <main className="grid min-h-screen place-items-center bg-cee-cream px-4 text-center">
      <section>
        <p className="text-sm font-semibold uppercase tracking-wide text-cee-red">
          CEE Admin
        </p>
        <h1 className="mt-3 text-3xl font-bold text-neutral-950">
          En construccion
        </h1>
      </section>
    </main>
  </React.StrictMode>,
);
